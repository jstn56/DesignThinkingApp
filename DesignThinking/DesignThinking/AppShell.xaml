<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:DesignThinking.Views"
       xmlns:proto="clr-namespace:DesignThinking.Views.Protocol"
       
       Title="DesignThinking"
       x:Class="DesignThinking.AppShell">
    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->
    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="LightGray"/>
                <Setter Property="Shell.ForegroundColor" Value="Black" />
                <Setter Property="Shell.TitleColor" Value="Black" />
                <Setter Property="Shell.DisabledColor" Value="#B4FFFFFF" />
                <Setter Property="Shell.UnselectedColor" Value="#95FFFFFF" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="LightGray" />
                <Setter Property="Shell.TabBarForegroundColor" Value="Black"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="#95FFFFFF"/>
                <Setter Property="Shell.TabBarTitleColor" Value="Black"/>
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>
    <TabBar>
        <ShellContent Title="DB" Icon="icon-template" ContentTemplate="{DataTemplate local:DashboardPage}" />
        <ShellContent Title="PM" Icon="icon-draft" ContentTemplate="{DataTemplate proto:ProtocolOverviewPage}" />
        <ShellContent Title="PR" Icon="icon-stopwatch" ContentTemplate="{DataTemplate local:ProjectPage}" />
        <ShellContent Title="KV" Icon="icon-user" ContentTemplate="{DataTemplate local:AccountManagementPage}" />
    </TabBar>
</Shell>
